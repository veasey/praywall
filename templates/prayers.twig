<!DOCTYPE html>
<html>
<head>
  <title>PrayWall</title>
</head>
<body>
  <h1>Prayers</h1>

  {% for prayer in prayers %}
    <div>
      <h2>{{ prayer.title }}</h2>
      <p>{{ prayer.body }}</p>
      <small>Posted: {{ prayer.date_posted }}</small>
    </div>
  {% else %}
    <p>No prayers yet.</p>
  {% endfor %}
</body>
</html>
