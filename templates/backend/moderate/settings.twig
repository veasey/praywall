{% extends "base.twig" %}

{% block content %}
  <h1>Moderator Settings</h1>

  <form method="post" action="/moderator/settings" class="moderator-settings-form">
    <div class="form-group">
      <label for="auto_approve">
        <input type="checkbox" id="auto_approve" name="auto_approve" {% if settings.auto_approve %}checked{% endif %}>
        Auto-approve prayers
      </label>
    </div>

    <div class="form-group">
      <label for="notify_email">
        <input type="checkbox" id="notify_email" name="notify_email" {% if settings.notify_email %}checked{% endif %}>
        Send email when a new prayer is submitted
      </label>
    </div>

    <button type="submit" class="save-button">Save Settings</button>
  </form>
{% endblock %}
